let result;
function min(a, b) {
    if (a < b) {
        return result = a;
    } else {
        return result = b;
    }
}

// const mini = min(a, b)
console.log(result);

min(2, 5);
min(3, -1);
min(1, 1);  